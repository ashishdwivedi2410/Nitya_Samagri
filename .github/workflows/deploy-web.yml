name: 🌐 Deploy Customer Web

on:
  push:
    branches: [main]
    paths:
      - "apps/web/**"
      - ".github/workflows/deploy-web.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/web/**"

env:
  REGISTRY:   docker.io
  IMAGE_NAME: nityasamagri/web
  NODE_VERSION: "20"

jobs:
  # ── Quality Check ────────────────────────────────────────────────────────────
  quality:
    name: 🔍 Type Check & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: package-lock.json

      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm run build
        env:
          NEXT_PUBLIC_API_URL:      https://api.livetestingserver.com/api/v1
          NEXT_PUBLIC_WS_URL:       wss://api.livetestingserver.com/ws
          NEXT_PUBLIC_RAZORPAY_KEY: ${{ secrets.RAZORPAY_KEY_ID }}

  # ── Build & Push Docker Image ─────────────────────────────────────────────────
  build:
    name: 🐳 Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: quality
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context:  .
          file:     ./apps/web/Dockerfile
          target:   production
          push:     true
          tags:     ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to:   type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_API_URL=https://api.livetestingserver.com/api/v1
            NEXT_PUBLIC_WS_URL=wss://api.livetestingserver.com/ws
            NEXT_PUBLIC_RAZORPAY_KEY=${{ secrets.RAZORPAY_KEY_ID }}
            NEXT_PUBLIC_APP_URL=https://livetestingserver.com

  # ── Deploy to Lightsail ───────────────────────────────────────────────────────
  deploy:
    name: 🚀 Deploy to Lightsail
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://livetestingserver.com

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key:      ${{ secrets.SERVER_SSH_KEY }}
          envs:     NEW_TAG
          script: |
            set -e
            cd ~/nityasamagri
            git pull origin main

            # Production only knows web_blue / web_green (see
            # docker-compose.prod.yml) — there's no bare "web" service.
            # This workflow deploys just the web app, so rather than doing
            # a full blue/green swap (which would also have to flip the
            # shared .active_color file that deploy-scheduled.yml uses to
            # keep api/web/admin in sync), it updates whichever color is
            # already live, in place.
            ACTIVE=$(cat .active_color 2>/dev/null || echo blue)
            echo "Currently live color: $ACTIVE — updating web_$ACTIVE in place."

            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$NEW_TAG
            docker tag  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$NEW_TAG ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:$ACTIVE

            docker compose -f devops/docker-compose.yml \
                           -f devops/docker-compose.prod.yml \
                           up -d --no-deps web_$ACTIVE

            sleep 10
            curl -sf https://livetestingserver.com || exit 1
            docker image prune -f
            echo "✅ Customer web deployed (color: $ACTIVE)!"
        env:
          NEW_TAG: ${{ github.sha }}

      - name: Smoke test
        run: |
          chmod +x scripts/smoke-test.sh
          ./scripts/smoke-test.sh
        env:
          SKIP_API:                "1"
          SKIP_ADMIN:              "1"
          SMOKE_TEST_RETRIES:      "6"
          SMOKE_TEST_RETRY_DELAY:  "8"

          